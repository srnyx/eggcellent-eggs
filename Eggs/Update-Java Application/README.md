# Java Application

This egg will update a JAR file from a folder then run the updated JAR file with Java

## Variables

- **JAR File:** The file that starts the application (example: Application.jar).
- **JAR Update Directory:** The directory that contains the JAR file to replace/update the main file.
